name: Build and Deploy Legion K Player

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libsdl2-dev libsdl2-image-dev libsdl2-ttf-dev g++-mingw-w64-x86-64

    - name: Build Linux Binary
      run: |
        cd Legion-K-Player
        make

    - name: Build Windows EXE
      run: |
        cd Legion-K-Player
        make win

    - name: Upload Artifacts
      uses: actions/upload-artifact@v3
      with:
        name: Legion-K-Player-Builds
        path: |
          Legion-K-Player/Legion-K-Player-Bin
          Legion-K-Player/Legion-K-Player.exe

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main')
    steps:
      - uses: actions/checkout@v3
      
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./Legion-K-Player/web
